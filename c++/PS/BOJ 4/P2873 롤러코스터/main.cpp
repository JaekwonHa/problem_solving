#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <iostream>
#include <algorithm>
#include <string>
#include <string.h>
#include <vector>
#include <queue>
#include <tuple>
#include <map>
#include <stack>
using namespace std;

#define INF 987654321
#define mod 1000000007
typedef long long ll;

int r, c;
char foot[1001][1001];
int m[1001][1001];


int main() {

	scanf("%d %d", &r, &c);
	for (int i = 1; i <= r; i++) {
		for (int j = 1; j <= c; j++) {
			scanf("%d", &m[i][j]);
		}
	}
	if (r % 2 == 1 && c % 2 == 0) {
		for (int i = 1; i<)
	}
	if (r % 2 == 0 && c % 2 == 1) {

	}
	if (r % 2 == 1 && c % 2 == 1) {

	}
	if (r % 2 == 0 && c % 2 == 0) {

	}

	return 0;
}